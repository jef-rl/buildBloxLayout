{
  "name": "new-project",
  "private": true,
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "type": "module",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "dev:playground": "npm run dev -w packages/playground",
    "build": "npm run build -w packages/framework && npm run build -w packages/playground  && npm run dev -w packages/playground",
    "build:all": "npm run build:framework && npm run build:playground",
    "build:framework": "npm run build -w packages/framework",
    "build:playground": "npm run build -w packages/playground",
    "clean": "rm -rf dist && rm -rf packages/framework/dist && rm -rf packages/playground/dist"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "autoprefixer": "^10.4.23",
    "caniuse-lite": "^1.0.30001766",
    "postcss": "^8.5.6",
    "typescript": "^5.9.3",
    "vite": "^7.3.1"
  }
}
